# 系统与策略变更记录

## 2026-08-10

### 研究报告目录整理

- `reports/` 增加总索引和归档说明；完整历史研究按结论摘要、优化原始证据、月度回放、日期重评
  和标准回放分层保存。
- 保留所有参数网格 JSON 和早期短样本报告以维持研究可追溯性，但明确
  `archive/pre_full_history/` 与旧 `ATR(21) x 4` 报告均已失效，不再代表当前基线。
- 当前冻结策略仍以 `STRATEGY.md` 为权威定义；报告目录只保存研究结论和生成证据，不保存行情
  数据、账户数据库或实盘记录。

### 历史数据增量同步

- Binance 拒绝超过近期窗口的 `aggTrades fromId` 查询时，导入器自动切换到数据库最新成交时间
  之后的 `startTime` 游标，避免历史 aggregate ID 无法继续增量同步。
- 本次变更仅维护研究数据导入路径；research-only 分支继续关闭实盘运行和订单提交。

## 2026-08-09

### 研究成果仪表盘

- 前端默认入口改为 SOXL 历史策略研究页，集中展示冻结基线、月度与 8 月逐日收益、动作锁候选、
  walk-forward 分段结果、ATR 参数邻域、研究决策和历史数据覆盖范围。
- 明确区分固定 15 分钟冻结基线与滚动 28–32 分钟候选；候选因留出段没有形成优势，保持未批准
  状态，不将样本内最高收益直接呈现为可部署策略。
- 顶栏固定显示研究分支和 `READ ONLY`，移除 PAPER/LIVE 切换、暂停、停止策略和平仓入口；次级
  页面统一改为历史回放、回测明细、历史绩效和历史数据语义。
- Playwright 覆盖桌面与移动端研究页、只读约束、历史回放图表和数据页，并验证页面没有横向溢出。
- 新增 5 月预热完成日至 8 月 8 日的连续账户策略与 SOXLUSDT 1x 买入持有累计收益对比图：策略
  `+171.85%`，标的 `-16.19%`；图中同时展示各月末净值和超额收益。

### 动作锁形式研究

- 在不改变 15 分钟 `ATR(32) × 3`、趋势效率 `8 / 0.25`、1.25x 风险预算和 5/2 bps 成本的条件下，比较无锁、固定 5/15/30/60/120 分钟时间桶以及成交后滚动 5/15/30/60 分钟冷却。
- 当前固定 15 分钟锁为 `+171.85%`、`-26.71%`回撤、124 笔交易；无锁为 `+131.12%`，证明动作锁具有实际价值。
- 滚动 30 分钟是最佳研究候选：完整收益 `+207.95%`、最大回撤 `-23.67%`、胜率 `46.55%`、利润因子 `1.646`。训练/验证/留出收益为 `+84.31% / +35.81% / +15.93%`，但留出与当前锁相同，尚不能证明前向优势。
- 滚动 20/25/35 分钟完整收益为 `+197.22% / +203.02% / +177.65%`，说明 20–35 分钟有平台，但当前继续保留固定 15 分钟锁，滚动 30 分钟仅作为未批准候选。

## 2026-08-08

### SOXL 策略适配性重评

- 增量导入 91,879 个 250 ms Tick 桶和 2,525,971 个底层成交，行情更新至 2026-08-08
  06:50:59 UTC；15 分钟 K 线连续，无缺口。
- 新增冻结参数诊断脚本，统一计算买入持有基准、持仓暴露、收益集中度、趋势与波动分层、成本
  压力及风险预算；脚本只读 `data/paper.db`，不访问交易 API。
- 当前 1.25x 基线的更新后完整收益为 `+171.85%`、最大回撤 `-26.71%`，但胜率仅
  `42.74%`，前 5 笔赢家贡献 `103.86%` 的净利润；20% 以上 24 小时高低区间的利润因子仅
  `1.03`。
- 此前未见的 17.6 小时尾段收益为 `-2.47%`，只有一笔完成交易。策略维持为研究基线，不因
  单笔新数据重新调参。
- 撤销 `2x × 70% = 1.40x` 的优化推荐状态，将其降为未批准研究候选；当前仍保持
  `2x × 62.5% = 1.25x`，在积累至少三个自然月和 100 笔前向交易之前不提高敞口。
- 重评报告增加连续账户的 UTC 月度 performance：5 月部分区间 `+33.32%`、6 月
  `+38.75%`、7 月 `+25.63%`、8 月截至 8 日 `+16.98%`。
- 固定当前信号和风险预算，复核两组 ATR 盈利保护、三个延续重入阈值及两组组合。
  `2.0 / 0.5 ATR` 保护虽提高胜率，却将完整收益从 `+171.85%` 降至
  `+40.96%`；单独重入未带来独立改善。
- `4.0 / 1.5 ATR` 盈利保护与 `1.4 ATR` 重入的组合仅保留为未批准研究候选；
  其训练收益高，但验证收益仅 `+4.27%`，低于基线 `+23.45%`。配置中盈利保护和
  延续重入仍保持关闭。

### 研究分支整理

- 本分支限定为历史数据维护、策略回测和生产候选参数研究；`live_futures.enabled` 与
  `allow_order_submission` 均关闭，不启动交易服务、不提交 Binance 订单。
- SOXL 原始历史压缩包集中保存到被 Git 忽略的 `data/history_soxl/`；导入脚本的默认归档路径
  已同步更新。导入后的 Tick、K 线和资金费率仍保存在 `data/paper.db`。
- 早期短样本报告移动到 `reports/archive/pre_full_history/`；删除已停用的旧策略文档、交易
  TODO 和过时的回测计划，完整历史研究报告保留在 `reports/soxl_perp_full_history/`。

## 2026-08-05

### 系统变化

- 完成 `SOXL/USDT PERP LIVE` 的 Binance USD-M Futures 实盘链路：签名账户校验、IP
  白名单检查、逐仓与双向持仓模式校验、杠杆校验、订单提交门禁及交易所对账。
- 增加实盘“停止策略”和“平仓”操作。停止状态持久化，重启后不会自动恢复；人工平仓要求
  二次确认，会重新读取 Binance 实际仓位，并且只发送减仓订单。
- 完善真实订单生命周期与幂等同步，避免同一交易所订单或成交被重复记录；网络状态不明确时
  先等待交易所对账，不重复提交订单。
- 实盘收益统计加入 Binance Futures 转入、转出现金流。外部注资不再计入策略收益，账户净值、
  策略盈亏和净入金分别展示。
- 胜率改为完整开仓—平仓轮次胜率，不再把单笔成交当成一次交易；多次分段成交会归入同一轮。
- paper 与实盘共用 ATR 盈利保护实现。策略状态包含盈利保护激活状态、持仓方向、入场 ATR、
  有利极值和保护线，服务重启后可以恢复。
- paper `soxl_perp` 重建回放使用与在线执行相同的盈利保护逻辑，并支持候选数据库、账户级
  替换、市场数据只读校验和新增行情尾部回放；不会替换 `soxlb` 或 `soxl_perp_long` 账本。
- Dashboard 增加盈利保护激活距离、动态跟踪距离和当前保护线展示；空仓或尚未激活时显示
  “未激活”。
- 外部固定下单金额、单日亏损和每日订单数量限制保持关闭；交易所规则校验、订单幂等、
  账户对账、减仓保护和 `30 bps` 滑点限制继续启用。

### 策略变化

- 基础信号继续使用 15 分钟 Tick 驱动的 `ATR(21) × 4` 跟踪策略、趋势效率过滤、单 K 线
  最多一次动作以及“先平仓、下一根 K 线确认后再反手”的规则。
- 新增稳健 ATR 盈利保护：持仓有利移动达到 `2.0 × 入场 ATR` 后激活。
- 激活后每个行情 Tick 使用 `0.5 × 当前 ATR` 计算动态保护距离，并持续更新持仓期间的
  有利极值。
- 盈利保护线只能向锁定更多利润的方向移动，不会后退；多仓与空仓使用对称计算。
- 触发盈利保护时只生成 `reduce_only` 平仓信号，不在同一信号内立即反手。实际成交仍遵循
  下一 Tick 成交模型；实盘使用 Binance 返回的真实成交结果。
- 持仓浮动盈亏和账户权益按最新行情动态计算。空仓时未实现盈亏为零，因此权益只会在新的
  成交、手续费、资金费或现金流出现时变化。
- ATR 止损和盈利保护均由本服务接收实时 Tick 后执行，不是预挂在 Binance 的原生止损单；
  策略执行依赖服务和行情连接持续在线。

### 回测与上线验证

- 同一数据截面下，基础版本收益 `+33.75%`、最大回撤 `-19.05%`；稳健版本收益
  `+24.99%`、最大回撤 `-4.44%`，共 10 轮交易、9 胜 1 负，其中 9 次由盈利保护退出。
- 该结果属于 2026-07-31 至 2026-08-05 的短样本内回放，包含配置的手续费、滑点、杠杆和
  历史资金费，不代表未来收益。
- paper `SOXL/USDT PERP` 已按稳健版本完成全历史重建：最终权益 `124,989.28 USDT`，
  累计收益 `+24.99%`，当前空仓且无挂单。
- 重建时发现并修复 `agg_trades` 行情时间索引缺项；生产数据库随后通过完整
  `PRAGMA integrity_check`。
- Python 完整测试共 137 项通过，前端生产构建通过；实盘服务在变更完成后恢复为
  `ARMED`，订单提交和 Binance 对账均正常。

详细对比报告见
[`reports/archive/pre_full_history/profit_exits/atr_profit_exit_comparison_20260805T151811Z.md`](reports/archive/pre_full_history/profit_exits/atr_profit_exit_comparison_20260805T151811Z.md)。

## 2026-08-06

### 策略变化

- 实盘 `SOXL/USDT PERP LIVE` 启用 `1.4 × 当前 ATR` 延续重入：策略完全平仓后，若下一根
  15 分钟 K 线继续沿原持仓方向运行达到阈值，并且趋势效率过滤通过，则按原方向重新开仓。
- 重入机会使用 Binance 真实平仓成交均价作为锚点，只在紧接平仓后的下一根 K 线有效；服务
  启动、历史成交同步和操作员人工平仓均不会创建重入机会。
- 重入成交视为全新的持仓，盈利保护按新成交价和新入场 ATR 重新初始化，继续共用
  `2.0 ATR` 激活、`0.5 ATR` 动态跟踪规则。

### 回测依据

- 更新至 2026-08-06 的完整样本中，关闭重入为 `+28.52%`、最大回撤 `-4.44%`；使用
  `1.4 ATR` 为 `+36.93%`、最大回撤 `-4.51%`。全样本仅出现 3 次重入，结果不代表未来
  收益；详细结果见 `reports/archive/pre_full_history/continuation_reentry/continuation_reentry_threshold_20260806.md`。

## 2026-08-07

### 完整历史数据与诊断

- 补齐 Binance `SOXLUSDT` 自上市以来至 2026-08-07 13:13:15 UTC 的行情：共
  9,973,473 个 250 ms Tick 桶、8,060 根已收盘 15 分钟 K 线和 252 条资金费率记录；数据库
  完整性检查通过。
- 原 `ATR(21) × 4`、`2.0 / 0.5 ATR` 盈利保护和 `1.4 ATR` 延续重入策略，在 200 根 K 线
  预热后的完整连续历史收益为 `-58.31%`，最大回撤 `-75.07%`。8 月 5-6 日记录的正收益只
  覆盖 7 月 31 日之后的短样本，不再代表完整历史表现。
- 主要问题是 `0.5 ATR` 盈利跟踪过早截断盈利交易，而基础 `4 ATR` 退出允许亏损继续扩大；
  空头交易在完整历史中也持续拖累结果。

### Walk-forward 优化

- 使用按时间顺序划分的训练集、验证集和留出集，依次搜索交易方向、ATR 周期与倍数、趋势
  效率过滤、反转确认、盈利保护和延续重入，共执行 606 次分段回放。留出集不参与调参。
- 最终选择长仓单向的 15 分钟 `ATR(32) × 3`，继续使用 8 根 K 线趋势效率和 `0.25` 最低
  效率门槛，关闭额外盈利保护和延续重入。
- 独立月份回放均为正：5 月 17-31 日 `+33.32%`、6 月 `+36.41%`、7 月 1-30 日
  `+23.45%`、7 月 31 日至 8 月 7 日留出集 `+17.04%`。
- 完整连续历史最终权益 `274,471.47 USDT`，累计收益 `+174.47%`，最大回撤 `-26.71%`，
  共 123 轮交易，胜率 `42.28%`，利润因子 `1.48`。详细方法和限制见
  `reports/soxl_perp_full_history/summaries/final_optimization_20260807.md`。

### 运行变化

- `soxl_perp` paper 与实盘信号改为长仓单向；实盘不再硬编码允许做空，长仓模式发现已有空仓
  会阻断策略下单，非减仓开空信号也会被拒绝。
- 标准回测未指定开始时间时，自动从完整预热区间之后开始，现可直接复现配置中的完整历史策略。
- 本次只修改代码和配置，未启动或重启实盘服务，也未向 Binance 提交订单。

### 多周期稳健性复核

- 在不固定 15 分钟周期或 8 根 K 线窗口的前提下，新增 5、15、30、60 分钟周期搜索，覆盖
  多空方向、ATR 周期与倍数、趋势窗口与阈值、反转确认和盈利保护，共完成 838 次分段回放。
- 60 分钟多空 `ATR(32) × 2`、趋势效率 `12 / 0.15` 是最高历史收益候选：统一起点连续收益
  `+495.91%`、最大回撤 `-27.63%`；但相邻窗口和 ATR 倍数存在明显表现断崖。
- 30 分钟候选在训练和验证中表现较强，但两个冻结版本在 7 月 31 日后的留出集分别为
  `-0.88%` 和 `-0.89%`，未通过最终检验。
- 当前 15 分钟只做多策略的留出集收益为 `+17.04%`，高于 60 分钟候选的 `+10.71%`，且
  分段和参数邻域更稳定。因此维持当前运行配置，不启用盈利保护或延续重入。
- 详细结果见
  `reports/soxl_perp_full_history/summaries/multitimeframe_optimization_20260807.md`。
