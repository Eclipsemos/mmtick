# TODO：实盘执行意图后续增强

## 当前问题与范围

2026-08-24 已完成当前 long-only 实盘路径的持久化执行意图、滑点恢复和可靠减仓。信号先落库，
订单进入 `SUBMITTING` 后才占用动作锁；429/418 会触发共享冷却，网络结果不明确时只按确定性
`clientOrderId` 对账。剩余工作集中在关闭中的兼容路径、审计字段和 Dashboard 可视化。

当前实盘为 SOXL long-only，做空、反向、利润保护和延续重入均关闭。因此先修复正在运行的
ATR 平仓与普通开多路径，再补齐关闭功能的兼容性。

## P0：风险降低型平仓可靠执行

- [x] `reduce_only` 完全绕过入场滑点、日内限制和盘口查询，按交易所实际完整仓位提交市价减仓。
- [x] 在任何外部下单请求前持久化确定性执行记录；交易所结果不明确时沿用现有
  `clientOrderId` 对账，不得直接重复提交。
- [x] 将信号生成与动作锁消费分离。只有本地订单成功进入 `SUBMITTING` 后才消费本 K 线动作；
  提交前失败不得留下动作锁。
- [x] 明确处理交易所拒单：若实际仓位仍存在，保存可审计的待处理退出并在完成对账后重试；若
  仓位已经归零则幂等结束。
- [x] 为盘口异常、明确拒单、模糊响应、部分成交、服务重启和重复 Tick 增加测试，证明 ATR
  `reduce_only` 最终只完成一次平仓且不会静默丢失。

## P1：Long-only 入场意图与滑点恢复

- [x] 引入持久化 `ExecutionIntent` 状态机：
  `PENDING → VALIDATING → SUBMITTING → ACCEPTED | COMPLETED | EXPIRED | CANCELED`。
- [ ] 意图至少保存账户、品种、方向、`reduce_only`、信号原因、信号价、ATR、跟踪线、策略版本、
  K 线起点、创建/过期时间、尝试次数、最近盘口与滑点、取消原因和确定性意图 ID。
- [x] 普通开多首次触发 `SLIPPAGE_LIMIT` 时不创建交易所订单、不消费动作锁；使用有界重试间隔
  检查待执行意图，盘口恢复到 `max_slippage_bps` 内后只提交一次。
- [x] 重试前重新验证账户仍空仓、无本地或交易所未决订单、实盘门禁正常、价格仍在 ATR 线上方、
  趋势效率仍通过且信号 K 线未结束；任一条件失效即记录原因并终止意图。
- [x] 意图 ID 按账户、策略版本、K 线、方向和信号类型生成，不依赖某次重试 Tick；数据库唯一
  约束或原子状态转换必须阻止并发执行者重复提交。
- [x] 区分可重试阻断（滑点、短暂盘口错误）和永久阻断（参数非法、订单低于最小规则、策略停止、
  仓位已变化）；永久阻断不得无限重试。
- [x] 服务重启时先对账交易所和本地订单，再恢复仍有效的意图；覆盖“交易所已接单但本地尚未
  更新”这一崩溃窗口。

## P2：兼容路径、审计与前端

- [ ] 在 P0/P1 稳定后，将相同意图语义扩展到目前关闭的 SHORT 入场、反向确认、利润保护和延续
  重入；状态不得在订单进入 `SUBMITTING` 前清除。
- [ ] 扩充 `ORDER_RISK_BLOCKED`、重试、过期和取消事件：记录意图 ID、信号类型、方向、信号价、
  bid/ask、实际滑点、阈值、尝试次数、状态转换和终止原因。
- [ ] LIVE Dashboard 显示“等待滑点恢复”的方向、当前滑点、阈值、首次触发时间、剩余有效时间
  和最近阻断原因；人工停止策略后明确显示意图已取消。
- [ ] 为 LONG/SHORT、反向、利润保护和延续重入增加对称测试，但不得为了测试重新启用生产配置。

## 验收门禁

- [ ] P0、P1 分别以独立提交完成，数据库迁移可重复执行且旧实盘库可无损升级。
- [x] 重复 Tick、重复回调、进程重启、部分成交和 Binance 模糊响应均不会生成重复订单。
- [ ] 完整运行 Pytest、Ruff、前端生产构建和 Playwright。
- [x] 部署前确认实盘空仓、无挂单并备份实盘数据库；部署后核对 `ARMED`、对账状态、意图表、
  订单状态和新增审计事件，再恢复策略。

## 完成标准

风险降低型平仓不依赖入场滑点或盘口门禁；有效开仓信号在滑点恢复后可安全续执行；所有信号从
产生、验证、提交到终止均可审计、可恢复，并通过确定性 ID 和原子状态转换保持订单幂等。
