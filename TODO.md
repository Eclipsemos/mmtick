# TODO：修复滑点拦截后的信号丢失

## 问题

当前策略在生成开仓、反手或延续重入信号时立即占用本 K 线动作锁，实盘执行层随后才检查
盘口滑点。若返回 `SLIPPAGE_LIMIT`，系统不会创建订单，也不会释放动作锁或保留待执行意图，
导致该信号通常无法在有效窗口内再次尝试。`reduce_only` 平仓虽然不受滑点阈值限制，但仍会
进行一次不必要的盘口查询。

## 实现任务

- [ ] 引入可持久化的 `EntryIntent`，保存原始信号方向、原因、信号价、ATR、创建时间、有效
  K 线、策略来源以及确定性意图 ID。
- [ ] 将“信号成立”和“动作锁已消费”分离。滑点拦截不得消费动作名额；只有订单成功创建并
  进入提交阶段后才锁定该 K 线。
- [ ] 对普通开仓、反向确认和延续重入统一使用待执行意图。反向及重入状态必须保留到订单成功
  提交或机会明确过期，不能在滑点检查前清除。
- [ ] 每个新 Tick 在无持仓、无交易所/本地未决订单且实盘门禁正常时重新检查待执行意图；盘口
  回到 `max_slippage_bps` 内即可提交。
- [ ] 重试前重新验证方向仍位于 ATR 止损线正确一侧、趋势过滤仍通过且有效 K 线尚未结束；
  条件失效时记录原因并取消意图。
- [ ] 使用原始意图 ID 生成稳定的 `clientOrderId`，保证重复 Tick、服务重启和网络结果不明确时
  不会产生重复订单。
- [ ] `reduce_only` 信号直接跳过入场滑点检查和盘口查询，继续使用完整实际仓位市价减仓；交易所
  拒单或结果不明确时仍按现有订单对账流程处理。
- [ ] 扩充 `ORDER_RISK_BLOCKED`/重试事件详情：信号类型、方向、信号价、bid/ask、实际滑点、
  阈值、意图 ID、尝试次数及过期原因。
- [ ] 在 LIVE Dashboard 显示“等待滑点恢复”的方向、当前滑点、阈值和剩余有效时间。

## 测试与验收

- [ ] 开仓首次滑点超限时不创建订单、不消费动作锁；盘口恢复后只提交一次。
- [ ] 普通开仓、LONG/SHORT 反向确认和延续重入均覆盖对称测试。
- [ ] 意图跨服务重启恢复；过期、趋势失效、已有持仓或人工停止时不会提交。
- [ ] 重复 Tick、重复回调和模糊 Binance 响应不会生成重复订单。
- [ ] ATR 止损和利润保护在极端盘口偏离及盘口查询异常时仍进入 `reduce_only` 提交流程。
- [ ] 完整运行 Pytest、Ruff、前端生产构建和 Playwright；部署前确认实盘空仓、无挂单，部署后
  核对 `ARMED`、对账状态和新增事件字段。

## 完成标准

滑点超限只会暂缓仍然有效的入场意图，不会静默丢失信号；风险降低型平仓不依赖入场滑点
门禁；所有重试均可审计、可恢复且保持订单幂等。
