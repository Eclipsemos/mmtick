# 小果量化 ATR 策略说明

## 1. 策略概述

**策略名称：** 小果量化 ATR Tick 实时策略  
**策略类型：** 趋势跟随 / ATR 移动止损  
**交易方向：** 仅做多  
**建议周期：** 15 分钟 K 线  
**信号检测：** 实时 Tick 检测  
**仓位方式：** 默认使用账户权益的 100%  
**反手交易：** 不允许

本策略利用 ATR（Average True Range，平均真实波幅）计算动态移动止损线。当价格实时上穿 ATR 止损线时买入；当价格实时下穿 ATR 止损线时平掉多头仓位并进入空仓状态。

---

## 2. 核心逻辑

### 2.1 ATR 计算

ATR 用于衡量市场波动率：

```text
ATR = ta.atr(ATR Period)
```

ATR 止损距离：

```text
止损距离 = ATR × Multiplier
```

默认参数：

```text
ATR Period = 7
Multiplier = 1.0
```

在 15 分钟图表中：

```text
ATR Period = 7
```

代表使用最近 7 根 15 分钟 K 线计算 ATR，即约 105 分钟的波动范围。

---

### 2.2 ATR 移动止损线

当价格处于多头趋势时，ATR 止损线位于价格下方，并且只能向上移动。

```text
新的止损线 = max(上一止损线, 当前价格 - ATR × Multiplier)
```

当价格处于空头或空仓趋势时，ATR 线位于价格上方，并且只能向下移动。

```text
新的止损线 = min(上一止损线, 当前价格 + ATR × Multiplier)
```

当价格穿越 ATR 线时，ATR 线切换方向。

---

## 3. 交易规则

### 3.1 买入条件

同时满足以下条件时建立多头仓位：

1. 当前 Tick 的价格位于 ATR 线上方。
2. 上一个 Tick 的价格位于 ATR 线下方。
3. 当前账户没有持仓。
4. 当前时间位于允许回测或交易的日期范围内。
5. 本根 K 线尚未执行过买入。
6. 本根 K 线尚未执行过空仓。

逻辑表达：

```text
价格实时上穿 ATR 线
且当前为空仓
且本根 K 线未交易
→ 买入
```

---

### 3.2 空仓条件

同时满足以下条件时平掉多头仓位：

1. 当前 Tick 的价格位于 ATR 线下方。
2. 上一个 Tick 的价格位于 ATR 线上方。
3. 当前持有多头仓位。
4. 本根 K 线尚未执行过空仓。

逻辑表达：

```text
价格实时下穿 ATR 线
且当前持有多头
→ 平仓并进入空仓
```

策略不会建立空头仓位。

---

## 4. 同一根 K 线反复穿越的处理

15 分钟 K 线内部可能出现多次上穿和下穿。为避免频繁交易，策略采用单根 K 线锁定机制。

### 执行规则

```text
第一次上穿 ATR → 买入
买入后第一次下穿 ATR → 空仓
空仓后再次上穿 ATR → 不再买入
下一根 K 线开始 → 解除锁定
```

### 示例

假设同一根 15 分钟 K 线中价格变化如下：

```text
ATR 下方
→ 上穿 ATR
→ 买入
→ 下穿 ATR
→ 空仓
→ 再次上穿 ATR
```

最后一次上穿不会重新买入，必须等待下一根 K 线开始。

这样可以防止：

```text
买入 → 空仓 → 买入 → 空仓 → 买入
```

在同一根 K 线内反复发生。

---

## 5. 实时执行方式

策略启用：

```pine
calc_on_every_tick = true
```

因此在实时行情中，每当 TradingView 收到新的价格 Tick，策略都会重新计算。

实时执行流程：

```text
收到新的行情 Tick
→ 更新当前价格
→ 更新实时 ATR 和止损线
→ 判断是否发生上穿或下穿
→ 生成交易信号
→ 提交策略订单
```

策略不会主动按照固定时间循环运行，而是在收到新的行情更新时执行。

---

## 6. 信号与成交的区别

图表上的绿色“买入”标签表示：

```text
策略检测到价格上穿 ATR 的时刻
```

TradingView 自动显示的蓝色 `Long` 标记表示：

```text
策略订单的模拟成交位置
```

市价买入订单通常在订单创建后的下一个可用 Tick 模拟成交，因此 `Long` 标记可能比“买入”信号晚一个 Tick。

实时交易时通常只是一个行情更新的差距，并不等于延迟一根完整的 15 分钟 K 线。

---

## 7. 图表显示

### ATR 线颜色

| 状态                    | ATR 线颜色 |
| ----------------------- | ---------- |
| 当前价格位于 ATR 线上方 | 红色       |
| 当前价格位于 ATR 线下方 | 绿色       |

### 信号标签

| 标签 | 含义                   |
| ---- | ---------------------- |
| 买入 | 价格上穿 ATR，建立多头 |
| 空仓 | 价格下穿 ATR，平掉多头 |

### 实时信息标签

图表右侧显示：

- 当前 ATR
- ATR × 倍数
- ATR 止损线价格
- 当前市场价格
- 当前价格与 ATR 线的距离
- 距离百分比
- 当前 ATR 状态
- 当前仓位状态
- 当前 K 线交易锁定状态

---

## 8. 策略参数

| 参数             | 默认值 | 说明                    |
| ---------------- | -----: | ----------------------- |
| ATR Multiplier   |    1.0 | 控制 ATR 止损线距离     |
| ATR Period       |      7 | ATR 使用的 K 线数量     |
| Source           |  Close | 用于计算穿越的价格源    |
| Position Size    |   100% | 每次使用账户权益的 100% |
| Pyramiding       |      0 | 禁止重复加仓            |
| Tick Calculation |   开启 | 实时行情按 Tick 计算    |
| Date Filter      |   开启 | 限制回测日期范围        |

---

## 9. 参数影响

### ATR Multiplier 较小

例如：

```text
0.5 ～ 0.8
```

特点：

- ATR 线距离价格更近
- 信号更灵敏
- 买卖次数更多
- 更容易受到震荡影响
- 止损速度更快

### ATR Multiplier 较大

例如：

```text
1.5 ～ 3.0
```

特点：

- ATR 线距离价格更远
- 信号数量减少
- 能容忍更大的价格波动
- 可能产生更大的单笔回撤
- 更适合明显趋势行情

### ATR Period 较小

特点：

- ATR 对近期波动变化更敏感
- 止损线调整更快
- 交易信号更多

### ATR Period 较大

特点：

- ATR 更平滑
- 信号更稳定
- 对短期价格变化反应更慢

---

## 10. 回测限制

实时 Tick 数据与历史 K 线数据存在区别。

### 实时行情

实时 K 线可以收到多个 Tick，因此策略可以识别：

```text
盘中上穿
盘中下穿
同一根 K 线中的多次穿越
```

### 历史行情

普通历史 15 分钟 K 线通常只有：

```text
开盘价
最高价
最低价
收盘价
```

TradingView 无法完整知道历史 K 线内部每个 Tick 的真实顺序，因此历史回测无法完全还原实时 Tick 策略。

例如，一根历史 K 线可能同时满足：

```text
最高价高于 ATR
最低价低于 ATR
```

但无法确定真实顺序是：

```text
先上穿再下穿
```

还是：

```text
先下穿再上穿
```

因此实时运行结果可能与图表刷新后的历史回测结果存在差异。

---

## 11. 提醒消息

### 买入提醒

```text
小果量化 ATR：买入
```

### 空仓提醒

```text
小果量化 ATR：空仓
```

提醒应设置为：

```text
Any alert() function call
```

Webhook 或自动交易系统接收到提醒后，应根据消息内容执行买入或平仓操作。

---

## 12. 策略流程图

```text
开始
  │
  ▼
计算 ATR 和 ATR 移动止损线
  │
  ▼
读取当前 Tick 价格
  │
  ├── 当前为空仓
  │       │
  │       └── 价格上穿 ATR？
  │               │
  │               ├── 否 → 继续等待
  │               │
  │               └── 是
  │                    │
  │                    ├── 本根 K 线已交易 → 忽略
  │                    │
  │                    └── 本根 K 线未交易 → 买入
  │
  └── 当前持有多头
          │
          └── 价格下穿 ATR？
                  │
                  ├── 否 → 继续持仓
                  │
                  └── 是 → 平仓并进入空仓
                                   │
                                   └── 本根 K 线禁止再次买入
```

---

## 13. 策略特点

### 优点

- 根据市场波动率动态调整止损线
- 趋势明确时可以持续持仓
- 实时 Tick 检测，避免必须等待 K 线收盘
- 仅做多，不承担做空风险
- 防止同一根 K 线频繁反复交易
- 可通过 TradingView Alert 接入自动交易系统

### 风险

- 横盘震荡时容易产生假突破
- ATR 参数对结果影响较大
- 杠杆 ETF 波动较高，可能快速触发止损
- 实时 Tick 结果不能通过普通历史 K 线完全复现
- 使用 100% 仓位时账户回撤可能较大
- 滑点、手续费和实际成交价格可能与回测不同

---

## 14. 当前策略总结

本策略是一套仅做多的 ATR 趋势跟随策略。

核心规则：

```text
价格实时上穿 ATR → 买入
价格实时下穿 ATR → 空仓
不做空
不反手
同一根 K 线空仓后不重新买入
下一根 K 线重新解锁
```

策略适合用于具有较强趋势和较高流动性的交易标的。对于 SOXL 等高波动杠杆 ETF，应重点测试 ATR 周期、ATR 倍数、手续费、滑点和最大回撤，并避免仅依据历史收益率判断策略有效性。